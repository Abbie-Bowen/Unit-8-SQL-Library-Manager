extends layout

block content
  h1 Books
  p 
    a.button(href='/books/new') Create New Book
 
  table
    tr
      th Title 
      th Author
      th Genre 
      th Year
      each book in books
        tr
          td #{book.title}
          td #{book.author}
          td #{book.genre}
          td #{book.year}